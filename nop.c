#include "monty.h"
/**
 * nop - substract the top two elements of the stack.
 * @stack: Double linked list
 * @line_number: Line counter
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void)stack;
	(void)line_number;
}
